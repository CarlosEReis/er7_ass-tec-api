
#spring.jpa.database=MYSQL
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

#spring.datasource.url=jdbc:mysql://localhost:3306/assistenciatec
spring.datasource.url=jdbc:mysql://localhost:3306/assistenciatec_teste?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}

# SECURITY
api.security.token.secret=${JWT_SECRET:my-secret-key}
api.security.cors.origin=${SECURITY_CORS}

# E-MAIL
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
